let age: number = 25;
let username: string;
let isDev: boolean = true;
let hobbies: string[];
type Person = {
  name: string;
  age: number;
};
let person: Person;
let people: Person[];
let course: string | number;

// Functions & types
function add(a: number, b: number) {
  return a + b;
}
